export type LocationItem = { id: number; warehouse: string; zone?: string | null; cell_code: string };

export type DocBase = { id?: number | string; type?: string; created_at?: string; meta?: any };
