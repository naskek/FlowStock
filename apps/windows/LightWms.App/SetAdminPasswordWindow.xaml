<Window x:Class="LightWms.App.SetAdminPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Установка пароля администратора"
        Height="240" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Задайте пароль администратора (минимум 6 символов)" TextWrapping="Wrap" />
        <StackPanel Grid.Row="1" Margin="0,6,0,0">
            <TextBlock Text="Пароль" />
            <PasswordBox x:Name="PasswordBox" Margin="0,2,0,0" />
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,6,0,0">
            <TextBlock Text="Повторите пароль" />
            <PasswordBox x:Name="ConfirmBox" Margin="0,2,0,0" />
        </StackPanel>
        <TextBlock x:Name="ErrorText"
                   Grid.Row="3"
                   Margin="0,6,0,0"
                   Foreground="Crimson"
                   TextWrapping="Wrap"
                   Visibility="Collapsed" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="110" Click="Save_Click" IsDefault="True" />
            <Button Content="Отмена" Width="90" Margin="8,0,0,0" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
