<Window x:Class="FlowStock.App.ItemTypeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Типы номенклатуры"
        SizeToContent="WidthAndHeight"
        MinWidth="840" MinHeight="460"
        MaxWidth="{x:Static SystemParameters.MaximizedPrimaryScreenWidth}"
        MaxHeight="{x:Static SystemParameters.MaximizedPrimaryScreenHeight}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border BorderBrush="#DDD" BorderThickness="1" Padding="10" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Название" VerticalAlignment="Center" />
                <TextBox x:Name="NameBox" Grid.Row="0" Grid.Column="1" Margin="8,0,12,8" />

                <TextBlock Grid.Row="0" Grid.Column="2" Text="Код" VerticalAlignment="Center" />
                <TextBox x:Name="CodeBox" Grid.Row="0" Grid.Column="3" Margin="8,0,12,8" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Сортировка" VerticalAlignment="Center" />
                <TextBox x:Name="SortOrderBox" Grid.Row="1" Grid.Column="1" Margin="8,0,12,8" />

                <CheckBox x:Name="IsActiveCheck" Grid.Row="1" Grid.Column="3" Margin="8,0,12,8" Content="Активен" IsChecked="True" />
                <CheckBox x:Name="VisibleInCatalogCheck" Grid.Row="2" Grid.Column="1" Margin="8,0,12,0" Content="Показывать в каталоге продукции" />
                <CheckBox x:Name="EnableMinStockCheck" Grid.Row="2" Grid.Column="3" Margin="8,0,12,0" Content="Контроль минимального остатка" />
                <CheckBox x:Name="EnableHuDistributionCheck" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="1" Margin="0,0,8,0" Content="HU распределение" VerticalAlignment="Center" />

                <StackPanel Grid.Row="0" Grid.RowSpan="3" Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Button x:Name="SaveButton" Content="Добавить" Width="100" Click="Save_Click" />
                    <Button Content="Новый" Width="90" Margin="8,0,0,0" Click="New_Click" />
                    <Button x:Name="DeleteButton" Content="Удалить" Width="90" Margin="8,0,0,0" Click="Delete_Click" />
                    <Button Content="Закрыть" Width="90" Margin="8,0,0,0" Click="Close_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid x:Name="ItemTypesGrid"
                  Grid.Row="1"
                  Margin="0,10,0,0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionChanged="ItemTypesGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Код" Binding="{Binding Code}" Width="150" />
                <DataGridTextColumn Header="Сорт." Binding="{Binding SortOrder}" Width="70" />
                <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="80" />
                <DataGridCheckBoxColumn Header="Каталог" Binding="{Binding IsVisibleInProductCatalog}" Width="90" />
                <DataGridCheckBoxColumn Header="Мин.остаток" Binding="{Binding EnableMinStockControl}" Width="110" />
                <DataGridCheckBoxColumn Header="HU распр." Binding="{Binding EnableHuDistribution}" Width="90" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
