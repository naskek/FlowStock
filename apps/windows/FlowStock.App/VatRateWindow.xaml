<Window x:Class="FlowStock.App.VatRateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ставки НДС"
        Width="760" Height="500"
        MinWidth="680" MinHeight="440"
        WindowStartupLocation="CenterOwner"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border BorderBrush="#DDD" BorderThickness="1" Padding="10" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Название" VerticalAlignment="Center" />
                <TextBox x:Name="NameBox" Grid.Row="0" Grid.Column="1" Margin="8,0,12,8" />

                <TextBlock Grid.Row="0" Grid.Column="2" Text="Ставка, %" VerticalAlignment="Center" />
                <TextBox x:Name="RateBox" Grid.Row="0" Grid.Column="3" Margin="8,0,12,8" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Сортировка" VerticalAlignment="Center" />
                <TextBox x:Name="SortOrderBox" Grid.Row="1" Grid.Column="1" Margin="8,0,12,0" />
                <CheckBox x:Name="IsActiveCheck"
                          Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2"
                          Margin="8,0,12,0"
                          Content="Активна"
                          IsChecked="True"
                          VerticalAlignment="Center" />

                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="5"
                            Margin="0,10,0,0"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <Button x:Name="SaveButton" Content="Добавить" Width="90" Click="Save_Click" />
                    <Button Content="Новая" Width="80" Margin="8,0,0,0" Click="New_Click" />
                    <Button x:Name="DeleteButton" Content="Удалить" Width="80" Margin="8,0,0,0" Click="Delete_Click" />
                    <Button Content="Закрыть" Width="80" Margin="8,0,0,0" Click="Close_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid x:Name="VatRatesGrid"
                  Grid.Row="1"
                  Margin="0,10,0,0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionChanged="VatRatesGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Ставка, %" Binding="{Binding Rate, StringFormat={}{0:0.####}}" Width="130" />
                <DataGridTextColumn Header="Сорт." Binding="{Binding SortOrder}" Width="80" />
                <DataGridCheckBoxColumn Header="Активна" Binding="{Binding IsActive}" Width="90" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
