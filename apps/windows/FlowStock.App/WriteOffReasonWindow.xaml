<Window x:Class="FlowStock.App.WriteOffReasonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Причины списания"
        Height="420" Width="640"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Код" VerticalAlignment="Center" />
            <TextBox x:Name="ReasonCodeBox" Grid.Column="1" Margin="6,0,12,0" />
            <TextBlock Grid.Column="2" Text="Наименование" VerticalAlignment="Center" />
            <TextBox x:Name="ReasonNameBox" Grid.Column="3" Margin="6,0,0,0" />
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,8">
            <Button Content="Добавить" Click="AddReason_Click" MinWidth="90" />
            <Button x:Name="DeleteReasonButton" Content="Удалить" Click="DeleteReason_Click" MinWidth="90" Margin="8,0,0,0" />
            <Button Content="Закрыть" Click="Close_Click" MinWidth="90" Margin="8,0,0,0" />
        </StackPanel>
        <DataGrid x:Name="ReasonsGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionChanged="ReasonsGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Код" Binding="{Binding Code}" Width="180" />
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
