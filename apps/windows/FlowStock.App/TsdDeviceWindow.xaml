<Window x:Class="FlowStock.App.TsdDeviceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ТСД устройства"
        Height="560" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <TextBlock Text="Устройства" FontWeight="SemiBold" />
            <DataGrid x:Name="DevicesGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Height="420"
                      Margin="0,6,0,8"
                      SelectionChanged="DevicesGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding DeviceId}" Width="*" />
                    <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="160" />
                    <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="90" />
                    <DataGridTextColumn Header="Последняя активность" Binding="{Binding LastSeen}" Width="200" />
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal">
                <Button Content="Обновить" Width="120" Click="Refresh_Click" />
                <Button Content="Новый" Width="120" Margin="8,0,0,0" Click="New_Click" />
                <Button x:Name="BlockButton" Content="Заблокировать" Width="140" Margin="8,0,0,0" Click="ToggleActive_Click" />
            </StackPanel>
        </StackPanel>
        <GroupBox Grid.Column="1" Header="Данные устройства" Margin="12,0,0,0">
            <StackPanel Margin="8">
                <TextBlock Text="ID устройства" />
                <TextBox x:Name="DeviceIdBox" Margin="0,2,0,8" />
                <TextBlock Text="Логин" />
                <TextBox x:Name="LoginBox" Margin="0,2,0,8" />
                <TextBlock Text="Пароль" />
                <TextBox x:Name="PasswordBox" Margin="0,2,0,8" />
                <CheckBox x:Name="IsActiveCheck" Content="Активен" Margin="0,0,0,8" />
                <TextBlock Text="Последняя активность" />
                <TextBox x:Name="LastSeenBox" Margin="0,2,0,8" IsReadOnly="True" />
                <Button Content="Сохранить" Width="140" Click="Save_Click" />
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
