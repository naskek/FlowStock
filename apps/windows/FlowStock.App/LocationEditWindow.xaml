<Window x:Class="FlowStock.App.LocationEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Место хранения"
        Height="280" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="ID" VerticalAlignment="Center" />
        <TextBox x:Name="IdBox" Grid.Row="0" Grid.Column="1" Margin="8,0,0,8" IsReadOnly="True" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Код" VerticalAlignment="Center" />
        <TextBox x:Name="CodeBox" Grid.Row="1" Grid.Column="1" Margin="8,0,0,8" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Наименование" VerticalAlignment="Center" />
        <TextBox x:Name="NameBox" Grid.Row="2" Grid.Column="1" Margin="8,0,0,8" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Макс. HU мест" VerticalAlignment="Center" />
        <TextBox x:Name="MaxHuSlotsBox" Grid.Row="3" Grid.Column="1" Margin="8,0,0,8" />

        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="8,12,0,0">
            <Button Content="Сохранить" Width="120" IsDefault="True" Click="Save_Click" />
            <Button Content="Отмена" Width="120" IsCancel="True" Margin="8,0,0,0" />
        </StackPanel>
    </Grid>
</Window>
